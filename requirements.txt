# ── RAG demo — pinned to the versions verified working on Python 3.14 ──
# ติดตั้งด้วย:  pip install -r requirements.txt

# --- Document loading (src/document_loader.py) ---------------------------
pymupdf==1.28.2                 # อ่าน PDF  (import fitz)
python-docx==1.2.0              # อ่าน .docx

# --- Chunking (src/text_splitter.py) -------------------------------------
pythainlp==5.3.5                # ตัดคำไทย (engine="newmm")
langchain-text-splitters==1.1.2 # RecursiveCharacterTextSplitter

# --- Embeddings (src/embedding_model.py) ---------------------------------
sentence-transformers==5.6.0    # โหลด BAAI/bge-m3
torch==2.13.0                   # backend ของ sentence-transformers
transformers==5.14.1
huggingface-hub==1.24.0

# --- Vector store & retrieval --------------------------------------------
faiss-cpu==1.14.3               # dense index (IndexFlatIP)
rank-bm25==0.2.2                # keyword index สำหรับ hybrid retrieval
numpy==2.5.1

# --- Generation (src/generator.py) ---------------------------------------
google-genai==2.19.0            # Gemini API
python-dotenv==1.2.3            # อ่าน GEMINI_API_KEY จาก .env
